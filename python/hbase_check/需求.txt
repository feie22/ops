巡检脚本需求：
1.确定如何获取集群IP地址
2.创建函数salt执行命令函数，参数为：ip列表，执行命令，返回为一个字典：{主机名：执行结果}，对执行结果进行合并，如果执行结果多于一个，则返回1
salt_result  salt_cmd_result

3.打印函数，参数：传入结果，参考值，正确值，错误值，删除文件
output_fuc

4.创建各检测函数，变量为：ip列表，执行命令，
调用salt执行命令函数，获取返回结果
调用打印参数，传入返回结果和参考值，正确及错误，删除文件

